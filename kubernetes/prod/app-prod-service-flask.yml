---
apiVersion: v1
kind: Service
metadata:
  name: app-prod-service-flask
spec:
  type: ClusterIP
  selector:
    app: wave-data-analysis
  ports:
    - name: flask-api-cluster
      port: 5000
      targetPort: 5000
...